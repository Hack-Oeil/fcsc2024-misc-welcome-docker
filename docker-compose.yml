version: '3.9'
services:
  ssh.cyrhades.fr:
    build: ssh
  webssh.cyrhades.fr:
    build: webssh
    ports:
    - ${PORT}:8888
  welcome-docker.cyrhades.fr:
    image: anssi/fcsc2024-misc-welcome-docker:latest
    ports:
      - ${PORT_SSH}:4000